{
  "schema": "synapse-competitor-identity-v1",
  "note": "Pinned identities of the benchmark-only competitor tools used in the owned-vs-competitor differentials. Competitor scores do not control owned-engine accuracy floors. The hosted acceptance workflow requires both comparisons to execute, verifies their versions and immutable artifact identities, and records their differential measurements. Other local test runs may skip a comparator that is unavailable.",
  "competitors": {
    "gitleaks": {
      "version": "8.30.1",
      "linux_x64_archive_sha256": "551f6fc83ea457d62a0d98237cbad105af8d557003051f41f3e7ca7b3f2470eb",
      "ruleset": "default built-in rules (no custom .gitleaks.toml)",
      "source": "https://github.com/gitleaks/gitleaks/releases/tag/v8.30.1",
      "dimension": "secrets",
      "scope_notes": "Invoked as `gitleaks detect -s <dir> --no-git -f json`; compared per file. The default rules carry provider-format rules that match the corpus's AWS/GitHub/GitLab/Slack/Stripe/GCP/SendGrid planted formats. gitleaks models the false-positive traps only through entropy and allowlist heuristics, not an explicit non-secret label, so trap handling is the primary axis of difference."
    },
    "checkov": {
      "version": "3.3.19",
      "image": "bridgecrew/checkov@sha256:d3e96adafdb315ca82e792ca8708c01adae85292800fb064c8b309b3d0cb7b80",
      "ruleset": "default built-in policies (Terraform and Kubernetes frameworks)",
      "source": "https://github.com/bridgecrewio/checkov/releases/tag/3.3.19",
      "dimension": "iac-misconfig",
      "scope_notes": "Invoked as `checkov -d <dir> --compact -o json --quiet`; compared per category by the checkov check id (CKV_AWS_*/CKV_K8S_*) that represents each category, so an unrelated policy firing on the fixed fixture is not counted as a false positive for that category."
    }
  }
}
